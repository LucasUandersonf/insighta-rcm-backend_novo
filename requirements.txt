fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
asyncpg>=0.29.0,<1.0
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[argon2]==1.7.4
alembic==1.13.2
slowapi==0.1.9
python-multipart==0.0.9

psycopg2-binary
asyncpg
greenlet

# --- dev/test (não necessário em produção) ---
pytest==8.3.3
pytest-asyncio>=0.24.0

# --- worker de ingestão (Etapa 1) ---
aioboto3==13.2.0
defusedxml==0.7.1

# --- Etapa 4: relatório semanal em PDF via WhatsApp ---
reportlab==4.2.5
httpx==0.27.2
bcrypt==4.1.3
passlib[bcrypt]

# --- Parser Inteligente de Contratos (IA) ---
pypdf==5.0.1

# --- Observabilidade (opcional — ver SENTRY_DSN em app/core/config.py) ---
# sentry-sdk detecta automaticamente Starlette/FastAPI instalados no mesmo
# ambiente (integração via entry point, sem precisar registrar nada
# manualmente) — só precisamos chamar sentry_sdk.init() com o DSN.
sentry-sdk==2.17.0
